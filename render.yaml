buildCommand: pip install -e .
startCommand: gunicorn --chdir app server:create_app() --bind 0.0.0.0:$PORT --workers 2 --timeout 120
envVars:
  - key: DATABASE_URL
    fromFilePath: /opt/render/project/data/store.db
  - key: PYTHON_VERSION
    value: "3.12"